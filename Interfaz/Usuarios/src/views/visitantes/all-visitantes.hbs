<div class="row">
  {{#each visitantes}}
  <div class="col-md-3">
    <div class="card">
      <div class="card-body">
         <h3 class="d-flex flex-row-reverse">
          <a href="/visitantes/edit/{{_id}}"><i class="fas fa-edit"></i></a> 
         </h3>
         Visitante:
        <h4 class="card-title d-flex justify-content-between align-items-center">
          
         {{ nombre }}
        </h4>
        <p>Curp: {{ curp }} </p>
        <p>No. Teléfono: {{ telefono }} </p>
        <p>Correo: {{ correo }} </p>
        <p>Nombre de Inquilino: {{ nombreInquilino }} </p>
        <p>Parentesco: {{ parentesco }} </p>
        <!-- DELETE REQUEST -->
        <form action="/visitantes/delete/{{_id}}?_method=DELETE" method="POST">
          <input type="hidden" name="_method" value="DELETE">
          <button type="submit" class="btn btn-danger btn-block btn-sm">Eliminar</button>
        </form>
      </div>
    </div>
  </div>
  {{else}}
  <div class="card mx-auto">
    <div class="card-body">
      <h1>Hola {{user.name}}</h1>
      <p clsss="lead">No hay visitantes.</p>
      <a href="/visitantes/add" class="btn btn-success btn-block">Crea un Visitante!</a>
    </div>
  </div>
  {{/each}}
</div>